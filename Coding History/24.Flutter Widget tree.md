Flutter Widget tree
===================

![captionless image](https://miro.medium.com/v2/resize:fit:1400/format:webp/1*wi_4D1VXMxQIAjQy_fi0Uw.png)

[Reference](https://medium.com/@ethan_june/flutter-widget-tree-c6adf67d785f)

by [Ethan June](https://medium.com/@ethan_june?source=post_page---byline--c6adf67d785f---------------------------------------)


4 days ago




Hello Guys ! Great Friday before weekend, since I rest long holiday I’ll keep focusing writting this weekend and Let’s get it started !

App Entry Point - (The OS Hook)
-------------------------------

```
void main() {
  runApp(const MyApp());
}
```

Every Mobile application they innated App entry point for calling **exact, known location** where it should begin running your application’s code.

It’s often where platform-specific setup or asynchronous initialization (like initializing Firebase or shared preferences) **before** the Flutter UI layer even begins to render.

Initializer - (The Configurator)
--------------------------------

```
class MyApp extends StatelessWidget {
  const MyApp({super.key});
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: 'Flutter Demo',
      theme: ThemeData(
        primarySwatch: Colors.blue,
      ),
      home: const MyHomePage(title: 'Flutter Home Page'), 
    );
  }
}
```

this is the **declaration of application’s identity**. It returns a configuration widget (`MaterialApp`) that tells the framework _how_ the app should look and behave (e.g., "use the Material design system," "use this blue theme," "start on this `MyHomePage` widget")

### MyApp({super.key})

Since Flutter’s foundational design principle: **“Everything is a Widget.”** it need to be distinctive. It can be easy to manage how a widget is uniquely identified within that tree, allowing manage the corresponding **Element Tree** efficiently.

### @override

s an **annotation** (or _attribute_) to **replace** or **redefine** a method inherited from a parent class or interface.

### Widget build

It's the function that converts your Dart code into the visual elements on the screen.

### (BuiildContext context) {

`**BuildContext**` **is an abstract class,** the **address** of a specific widget within the tree structure. Every single widget has its own `BuildContext`.

**It’s Passed by the Framework:** The Flutter framework creates and passes a new `BuildContext` to the widget every time the `build` method is called.

**Its Primary Job is to Look Up:** The main reason a widget needs its `context` is to **find other widgets or resources**higher up in the tree (its ancestors).

Today I explained most basic level of Flutter widget tree which are not mostly explained why actually script in the structure, I hope more developer easy to acquisite flutter theory !